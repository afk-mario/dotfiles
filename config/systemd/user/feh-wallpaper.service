[Unit]
Description=Random wallpaper with feh

[Service]
Type=oneshot
EnvironmentFile=%h/.wallpaper
ExecStart=/bin/bash -c '/usr/bin/feh --bg-max --no-xinerama "$@" --randomize "/home/arlefreak/Media/Wallpapers/"'

[Install]
WantedBy=default.target
